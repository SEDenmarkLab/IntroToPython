For all the following exercises, you should be considering as many boundary cases as possible, adding type hints, and adding full documentation, preferrably with `numpy` style docstrings.

## Exercise 6.1

* Create a function that opens [Tutorial6.csv](./Tutorial6.csv) in native Python and creates an IR (infrared) spectrum directly from the file using matplotlib.
  * You should do this with both the `csv` module and using read/write operations.
  * You are not allowed to use `pandas` or `numpy`
  * Note: The x-axis starts at 4000 and goes down to 500. Also the "-1" should be superscripted on the x-axis

## Exercise 6.2

* Do the same thing you did in **Exercise 6.1**, but use `pandas`/`numpy` in combination with `matplotlib`

## Exercise 6.3

* Create a violin plot representing the different types of alkenes within the file [SADExample.csv](./SADExample.csv)
