umpy`

## Exercise 3.1

* Create a function tthat takes any list of numbers (floats/ints) and returns the median.
  * Once you accomplish this, you are allowed to download/use `numpy`, and attempt to do this within a single line. Feel free to jump into [Unit 6](../Unit6/Unit6Q.md) for additional questions regarding auxilary packages that may be useful regarding
    * You are free to reorganize/sort the list in a single line before you do this.

## Exercise 3.2

* Create a function  that integrates the "Bubble Sort" algorithm for any list of integers.
  * You may look up what bubble sort is, but try to avoid looking up a direct implementation

## Exercise 3.3

* Create a function that times the differences of nested for loops up to five nested for loops.
  * Use the `time` module, and look at both wall clock time and cpu time (make sure you know the difference!).
    * You can make the operation as simple as `time.sleep()`
  * This function should have a depth (i.e. number of nested loops), and a size (number of iterations).

#### Exercise 3.3 Challenge:

You can manually code this, OR, if you're not looking to write out 5 unique nested for loops, take a look into the concept of recursion, and see if you could write a function that expands this to any number of nested loops.
